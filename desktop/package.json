{
  "name": "pulldex-desktop",
  "version": "0.3.1",
  "description": "PullDex — Pokémon TCG Living Pokédex Tracker",
  "main": "main.js",
  "author": "PullDex",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win --config electron-builder.yml -c.win.signAndEditExecutable=false && node scripts/apply-icon.js && electron-builder --win nsis --config electron-builder.yml -c.win.signAndEditExecutable=false --prepackaged release/win-unpacked",
    "pack": "electron-builder --dir --config electron-builder.yml -c.win.signAndEditExecutable=false && node scripts/apply-icon.js"
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "31.4.0",
    "electron-builder": "24.13.3"
  },
  "build": {
    "extends": null
  }
}
