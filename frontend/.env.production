# Production environment for PullDex frontend.
#
# When VITE_API_URL is empty, the API client uses the same origin as the
# page (same-origin requests).  In desktop mode, the React frontend is
# served by FastAPI itself, so all API calls go to the same host:port.
VITE_API_URL=
